<html lang="en">

<head>
  <meta charset="utf-8">
  <meta content="width=device-width, initial-scale=1.0" name="viewport">

  <title>Admin Login - CWA</title>
  <meta content="" name="description">
  <meta content="" name="keywords">

  <!-- Favicons -->
  <link href="/img/favicon.png" rel="icon">
  <link href="/img/apple-touch-icon.png" rel="apple-touch-icon">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css">

  <!-- Google Fonts -->
  <link
    href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Raleway:300,300i,400,400i,500,500i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i"
    rel="stylesheet">

  <!-- Vendor CSS Files --> 

  <link href="cwa/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
  <link href="cwa/vendor/bootstrap-icons/bootstrap-icons.css" rel="stylesheet">
  <link href="cwa/vendor/boxicons/css/boxicons.min.css" rel="stylesheet">
  <link href="cwa/vendor/glightbox/css/glightbox.min.css" rel="stylesheet">
  <link href="cwa/vendor/swiper/swiper-bundle.min.css" rel="stylesheet">

  <!-- Template Main CSS File -->
  <link href="cwa/css/style.css" rel="stylesheet">

</head>

<body>
  <%- include('../common/header') %>
    <main id="main">
      <section id="contact" class="contact">
        <div class="container" data-aos="fade-up">

          <div class="section-title">
            <h2>Admin Login </h2>
            <p>Welcome To Admin Login Page</p>
          </div>
          <div class="row mt-5">
            <div class="col-lg-4">
              <div class="info">
                <div class="address">
                  <i class="bi bi-envelope"></i>
                  <h4>Email:</h4>
                </div>
                <div class="email">
                  <i class="bi bi-lock-fill"></i>
                  <h4>Password:</h4>
                </div>
              </div>
            </div>
            <div class="col-lg-8 mt-5 mt-lg-0">
              <form action="/adminLogin" method="post" role="form" class="php-email-form" id="loginForm">
                <div class="row">
                  <div class="col-md-6">
                    <input type="email" class="form-control" name="email" id="email" placeholder="Your Email" required>
                  </div>
                </div>

                <div class="col-md-6 mt-3">
                  <div class="input-group">
                    <input type="password" class="form-control" name="password" id="password" placeholder="Password"
                      required>
                    <span class="input-group-addon">
                      <span id="password-toggle" class="password-toggle"><i class="far fa-eye"></i></span>
                    </span>
                  </div>
                </div <div><button type="submit" id="loginButton">Login</button>
            </div>
            </form>
          </div>
        </div>

        </div>
      </section><!-- End Contact Section -->
    </main><!-- End #main -->



    <!-- Template Main JS File -->
    <script src="cwa/js/main.js"></script>
    <script>
      // Add this JavaScript code to your existing script or create a new JavaScript file
      const passwordInput = document.getElementById('password');
      const passwordToggle = document.getElementById('password-toggle');

      // Function to toggle password visibility
      function togglePasswordVisibility() {
        if (passwordInput.type === 'password') {
          passwordInput.type = 'text';
        } else {
          passwordInput.type = 'password';
        }
      }

      // Add click event listener to the password toggle icon
      passwordToggle.addEventListener('click', togglePasswordVisibility);

      function validatePassword(password) {

        if (password.length < 8 || password.length > 20) {
          return false;
        } z
        return true;
      }
      const loginForm = document.getElementById("loginForm");
      loginForm.addEventListener("submit", function (event) {
        const passwordInput = document.getElementById("password");
        const password = passwordInput.value;

        if (!validatePassword(password)) {
          alert("Password does not meet the criteria. Password must be 8 to 20 characters long.");
          event.preventDefault();
        }
      });
    </script>
</body>

</html>